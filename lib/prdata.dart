import 'package:flutter/material.dart';
class PrData extends StatelessWidget {
  //const PrData({Key? key}) : super(key: key);

  final String pname;

  PrData({required this.pname});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
